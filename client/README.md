# CheckInn Client

React + Vite frontend for CheckInn, an event registration and QR attendance system.

## Scripts

```bash
npm run dev
npm run build
npm run lint
```

Set `VITE_API_URL` in `.env` to point to the Express API.
